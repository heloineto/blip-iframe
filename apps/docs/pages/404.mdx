import { Layout } from '@/layout';
import { MDX_DATA } from '@/mdx';

export default Layout(MDX_DATA.NotFound);

# 404

Nothing found. If some of the Blip Iframe documentation pages are lining to
this page, please let us know by opening an [issue](https://github.com/heloineto/blip-iframe/issues/new).
Otherwise, get back to the [home page](/).
